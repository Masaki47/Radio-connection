<!-- フォローボタン ------------------------------------------------------------------>
<%= form_for(current_member, url: relationships_path, method: :post, remote: true) do |f| %>
  <%= hidden_field_tag :following_id, @member.id %>
  <%= f.submit "フォローする", class: "btn btn-outline-secondary" %>
<% end %>