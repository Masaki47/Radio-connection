<!-- フォロー解除ボタン ------------------------------------------------------------------>
<%= form_for(current_member, url: relationship_path(@member), method: :delete, remote: true) do |f| %>
  <%= f.submit "フォロー解除", class: "btn btn-outline-secondary" %>
<% end %>